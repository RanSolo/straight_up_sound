%h1 Dashboard

#user
  %h3 User Profile
  %p= @user.last_name
  %p= @user.username
  %p= @user.phone
  %p= @user.email

#songs
  %h3 Songs booty
  - @songs.each do |song|
    %p= song.title
    -# %p link_to song.title, songs_path(song.id)
  = render "users/shared/song_form"

#recordings
  %h3 Recordings
  - @recordings.each do |recording|
    %p= recording.submitted_at
    %p= recording.play_count
    %p= recording.file
