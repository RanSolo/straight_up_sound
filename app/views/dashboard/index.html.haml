%h1 Dashboard

#user
  %h3 User Profile
  %p #{@user.first_name} #{@user.last_name}
  %p= @user.phone
  %p= @user.email

#songs
  -# = link_to "Create a song", songs_path
  %h3 Songs
  -# = simple_form_for @song do |f|
  -#   = f.input :song_title, placeholder: "song title", autofocus: true
    -# = f.input :lyric, placeholder: "lyric"
  -#   = f.input :password, placeholder: "password"
  -#   = f.button :submit, "Create song"
  - @songs.each do |song|
    %p link_to "#{song.title} #{song.comments.count}", songs_path(song.id)

#recordings
  %h3 Recordings
  - @recordings.each do |recording|
    %p= recording.submitted_at
    %p= recording.play_count
    %p= recording.file
